#include "main.h"

/**
  * mul - Multiplies two integers values
  * @x: The first digit to multiply
  * @y: The second digit to multiply with first digit
  *
  * Return: The result of multiplication between x value and y value
  */
int mul(int x, int y)
{
	return (x * y);
}

