#include "main.h"

/**
 * _islower - Checks if a character is a lowercase letter
 * @c: Input character
 *
 * Return: 1 if c is a lowercase letter, 0 otherwise
 */
int _islower(int c)
{
    return (c >= 'a' && c <= 'z');
}
